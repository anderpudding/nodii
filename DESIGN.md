# DESIGN.md — Nodii 비주얼 기준

> 버전 1.0 · 2026-09-17 · 근거: `docs/01-requirements.md` v1.1, `docs/02-system-design.md` v1.2 §7
> 이 파일은 **왜 이렇게 생겼는가**를 담습니다. 정확한 값과 컴포넌트 스펙은 `docs/design/`에 있습니다.
> 화면 시안(아트보드)은 Claude 디자인 캔버스 "Nodii 디자인"에 있고, 이 문서와 `docs/design/`이 시안보다 우선입니다.

---

## 1. 한 줄 요약

**색은 목표가 담당하고, 나머지는 흰 바탕과 잉크로 조용히 둔다.** 투두메이트처럼 밝고 가볍지만, 기록을 지우지 않고 되돌릴 수 있는 앱으로 보이게 합니다.

## 2. 원칙

1. **강조색을 따로 두지 않습니다.** 목표 색이 유일한 색이고, 체크·이름표·미리보기가 그 색을 씁니다. 버튼과 오늘 표시는 잉크(거의 검정)입니다.
2. **지난날과 앞날을 다르게 그립니다.** 캘린더 도장은 지난날이면 채우고 앞날이면 테두리만 그립니다. 놓친 날이 먼저 눈에 띄어야 합니다.
3. **되돌릴 수 있게 보입니다.** 옮기기·삭제·가져오기·건너뛰기는 실행 취소 토스트를 5초간 띄웁니다. 확인 창은 "무엇이 함께 사라지는지"를 개수로 알려줍니다.
4. **맥 앱처럼 동작합니다.** 우클릭 메뉴, `⌘N` 같은 단축키, 시스템 글꼴 크기 감각, 라이트·다크 모두 지원.
5. **HEX를 직접 넣어도 무너지지 않습니다.** 목표 색은 사용자가 정하므로, 글자색과 체크색은 대비를 계산해서 고릅니다(§4).

## 3. 색

- 바탕은 흰색(`bg`), 사이드바는 아주 옅은 회색(`side`). 크림색·베이지 계열은 쓰지 않습니다.
- 글자는 `ink` → `ink-2` → `muted` 3단계. 회색은 한 계열(쿨 뉴트럴)만 씁니다.
- 파랑(`focus`)은 **포커스 링과 선택 상태 전용**입니다. 장식으로 쓰지 않습니다.
- 빨강(`danger`)은 삭제와 오류 전용, 일요일 날짜 숫자에만 예외적으로 같은 계열을 씁니다.
- 정확한 값: `docs/design/tokens.md`.

## 4. 목표 색 규칙 (구현 필수)

목표 색은 프리셋 11개 + HEX 직접 입력(GOAL-02, GOAL-03)입니다. 어떤 색이 들어와도 읽히도록 두 함수를 `@nodii/core`에 순수 함수로 둡니다.

| 함수                             | 하는 일                                                                                            |
| -------------------------------- | -------------------------------------------------------------------------------------------------- |
| `goalTint(goalColor, bg, ratio)` | 이름표 바탕색. 바탕에 목표 색을 라이트 14%, 다크 22% 섞은 값                                       |
| `goalInk(goalColor, bg, isDark)` | 이름표 글자색. 목표 색을 잉크(다크면 흰색) 쪽으로 5%씩 섞어, 이름표 바탕과 대비 4.5:1이 되는 첫 색 |
| `checkColor(goalColor)`          | 체크 표시 색. 흰색 대비가 3:1 이상이면 흰색, 아니면 잉크                                           |

- 순수 계산이므로 `core`에 두고 단위 테스트를 씁니다(NFR-10). 프리셋 11색 + 극단값(#FFFFFF, #000000)을 케이스로.
- CSS `color-mix()`는 macOS 12의 WebView에서 지원되지 않을 수 있어 **쓰지 않습니다.** 값은 TypeScript에서 계산해 인라인 스타일로 넘깁니다.

## 5. 타이포그래피

- 한 가지 글꼴만 씁니다: **Pretendard Variable**(OFL). 앱에 번들해서 오프라인에서도 같은 화면이 나오게 합니다.
- 숫자는 `font-variant-numeric: tabular-nums`. 캘린더 숫자, 남은 개수, 버전, 카운트다운에 적용합니다.
- 크기 단계는 6개뿐입니다(`docs/design/tokens.md`). 화면마다 새 크기를 만들지 않습니다.

## 6. 모션

- 상태 변화 0.15초, 메뉴 0.14초, 토스트 0.24초. 그 이상 긴 연출은 없습니다.
- 유일하게 눈에 띄는 움직임은 **체크**입니다: 누를 때 0.86배로 눌리고, 체크 표시가 0.28초에 걸쳐 튀어나옵니다.
- `prefers-reduced-motion: reduce`이면 모든 애니메이션을 끕니다.

## 7. 접근성 기준 (지켜야 통과)

- 본문 대비 4.5:1, 24px 이상 큰 글자 3:1. 빈 체크 박스 테두리와 입력칸 테두리는 바탕 대비 3:1.
- 모든 버튼·링크·입력에 보이는 포커스 링(2px `focus` 색, offset 2px). `outline: none`만 두는 코드는 금지.
- 아이콘만 있는 버튼에는 `aria-label`. 체크박스는 `aria-pressed`, 메뉴는 `aria-haspopup`/`aria-expanded`, 확인 창은 `role="alertdialog"`.
- 색만으로 상태를 나타내지 않습니다(완료는 색 + 체크 + 취소선, 건너뜀은 점선 + "건너뜀" 글자).

## 8. 하지 않는 것

- 강조색 그라데이션, 네온 글로우, 유리 효과, 장식용 점(●)과 이모지 아이콘.
- 크림/베이지 바탕 + 테라코타 강조 조합.
- 아이콘 라이브러리 혼용. 아이콘은 한 세트(2~2.4px 스트로크)만 씁니다.
- 앞날과 지난날을 같은 모양으로 그리는 캘린더.
