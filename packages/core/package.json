{
  "name": "@nodii/core",
  "version": "0.1.0",
  "private": true,
  "description": "Nodii 도메인 로직: 타입, 반복 규칙 계산, 날짜 유틸 (UI·네트워크 의존성 없음)",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^5.0.1",
    "vitest": "^5.0.1"
  }
}
