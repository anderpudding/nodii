# 화면 스펙

> 요구사항 ID는 `docs/01-requirements.md`, 화면 구조는 `docs/02-system-design.md` §7 기준.
> 각 화면 이름 옆의 괄호는 디자인 캔버스의 아트보드 이름입니다.

## 메인 (`Main`, `MainDark`) — 1120×740

```
┌ 사이드바 380 ──────────┬ 하루 목록 ─────────────────┐
│ ● ● ●                  │            [오프라인] [설정] │
│ 2026년 9월  ◀ ▶ [오늘] │ 9월 17일 목요일              ◀ ▶ │
│ 일 월 화 수 목 금 토   │ 오늘, 6개 중 2개 끝냄          │
│ [도장/날짜 6줄]        │ ┌ 지난 미완료 3개 [오늘로 가져오기] ┐│
│ ─────────────────      │ (공부 +)                      │
│ 목표 관리 / 루틴 관리  │  ☑ 알고리즘 2문제        ⋯   │
└────────────────────────┴───────────────────────────┘
```

- 제목 아래 요약: 오늘이면 `오늘, N개 중 M개 끝냄`, 아니면 `N개 중 M개 끝냄`, 비었으면 `할 일 없음`.
- 할 일이 하나도 없는 날은 목표 이름표 위에 한 줄: "이날은 비어 있어요. 목표 이름을 누르면 할 일을 바로 추가할 수 있어요."
- 지난 미완료 배너는 **오늘을 보고 있을 때만** 뜨고, 가져오면 각 목표의 맨 아래에 붙습니다(TODO-10).
- 오프라인이면 타이틀바 오른쪽에 "오프라인이라 보기만 할 수 있어요"(SYNC-04).
- 사이드바 아래 메뉴: 목표 관리 → `GoalSheet`, 루틴 관리 → `RoutineList`. 설정 아이콘 → `Settings`.

## 로그인 (`Login`) — 880×600

- 1단계: 이메일 입력 + "코드 받기". 2단계: 6칸 코드 입력 + "로그인".
- 재전송은 60초 카운트다운 뒤 "코드 다시 받기"로 바뀝니다(AUTH-07). 옆에 "다른 이메일로".
- 오류 문구는 `role="alert"`로: 틀린 코드 → "코드가 맞지 않아요. 메일의 숫자를 다시 확인해 주세요.", 만료 → "코드 유효 시간(10분)이 지났어요. 새 코드를 받아 주세요."
- 6칸이 다 차기 전에는 로그인 버튼이 비활성입니다.
- 심사용 데모 계정(AUTH-08)의 비밀번호 입력칸은 해당 이메일일 때만 나타납니다. 일반 사용자에게는 보이지 않습니다.

## 목표 관리 (`GoalSheet`) — 시트, 창 위에서 내려옴

- 줄: 드래그 손잡이 · 목표 이름표 · "할 일 42개 · 루틴 1개" · [보관].
- 줄을 누르면 아래로 펼쳐져 이름 입력, 프리셋 11색, HEX 입력(형식이 틀리면 테두리 `danger` + 안내), [목표 삭제].
- 마지막 남은 활성 목표는 보관·삭제 버튼이 비활성이고 `title`로 이유를 알려줍니다(GOAL-07).
- 삭제 확인 창: "'공부' 목표를 삭제할까요?" + "할 일 42개와 루틴 1개가 모든 날짜에서 함께 삭제돼요. 지난 기록을 남기려면 보관을 써 주세요." + [대신 보관하기] [취소] [삭제].
- 아래에 "보관한 목표" 구역과 [보관 해제].

## 루틴 편집 (`RoutineEditor`) — 모달

- 제목 · 목표(이름표 중 고르기) · 반복(매일/매주/매월 세그먼트) · 간격 스테퍼 · 요일 또는 날짜 고르기 · 시작일/종료일 · **다음 5회 미리보기**.
- 매월에서 없는 날짜는 말일에 나타난다는 안내를 붙입니다(ROUT-04).
- 요일이나 날짜를 하나도 고르지 않으면 미리보기 자리에 안내가 뜨고 저장이 비활성입니다.
- 미리보기 첫 칸은 목표 색 이름표 톤, 나머지는 평범한 칩.

## 루틴 규칙 적용 범위 (`RoutineScope`) — 확인 창

- 규칙(주기·요일·일·시작일)을 바꿔 저장할 때만, 그리고 시작일이 오늘보다 이전일 때만 뜹니다(ROUT-08).
- 맨 위에 바뀐 내용: `매주 화·목·토 → 매주 월·수·금`.
- 선택지 두 개, 각각 아래에 **2주치 미니 캘린더**로 결과를 보여줍니다(끝낸 날/기록 없음/예정 범례 포함).
  - 오늘부터(기본): "어제까지의 규칙과 완료 기록은 그대로 두고, 9월 17일부터 새 규칙으로 보여요."
  - 과거 날짜까지 모두: "시작일(9월 1일)부터 새 규칙으로 다시 계산해요. 새 규칙에 맞지 않는 날의 완료 기록은 지우지 않고 숨겨요."
- 확인 버튼 문구는 고른 선택지에 따라 "오늘부터 적용" / "모두 적용".

## 루틴 관리 (`RoutineList`) — 시트

- 목표별로 묶고 줄마다 제목과 "매주 화·목·토, 9월 1일부터". 호버 때 [편집] [그만두기].
- [그만두기] → 확인 창에서 둘 중 하나(ROUT-09):
  - 오늘부터 그만하기: "어제까지의 기록과 표시는 캘린더에 그대로 남아요."
  - 완전히 삭제: "모든 날짜에서 사라지고, 완료 기록도 더 이상 보이지 않아요."
- 아래에 "끝난 루틴" 구역: 기간과 "기록은 캘린더에 남아 있어요".
- 오른쪽 위 [새 루틴] → `RoutineEditor`.

## 설정 (`Settings`) — 별도 창 880×600

- 계정: 이메일 표시, 로그아웃(캐시도 지운다는 안내), 계정 삭제(AUTH-06).
- 보기: 주 시작 요일(일/월), 테마(시스템/라이트/다크).
- 단축키 안내: `⌘N` 새 할 일 · `⌘T` 오늘로 · `←/→` 이전·다음 날 · `⌘,` 설정(SET-03).
- 정보: 개인정보처리방침 · 이용약관 · 지원 페이지(모두 `nodii.app` 아래, 실제 주소로 교체 필요) · 버전.
- 계정 삭제 확인 창: 지워질 개수를 적고, 이해 체크를 켜야 삭제 버튼이 켜집니다.

## 업데이트 필요 안내 (`UpdateRequired`) — 전체 화면

- 로그인 전에 뜨고 앱을 막습니다(SET-05, §6.7).
- "새 버전이 필요해요" + 데이터는 그대로라는 안내 + [App Store에서 업데이트] + 지금 버전 / 필요한 버전.
- 버전을 읽지 못하면(오프라인 등) 이 화면을 띄우지 않고 그냥 진행합니다.

## 아직 안 그린 것

- 드래그로 순서 바꾸는 중의 모습(GOAL-04, TODO-06)
- 첫 로그인 직후의 빈 상태(기본 목표 "할 일" 하나만 있는 화면)
- 저장 실패 토스트의 실제 문구 모음
